name: Architecture Guard

on:
  pull_request:
  push:

jobs:
  architecture-check:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5

      - name: Run Architecture Guard
        run: python scripts/architecture_guard.py
